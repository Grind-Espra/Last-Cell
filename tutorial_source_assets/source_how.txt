const finalBoard=Array.from({length:30},(_,i)=>`<div class="example-cell ${i===14?"killer":""} ${i===29?"victim":""}">${i===29?"☠":i===14?"А":i%6===0?"Б":i%7===0?"В":""}</div>`).join("");const crossCells=Array.from({length:25},(_,i)=>{const r=Math.floor(i/5),c=i%5;return `<i class="${r===2||c===2?"row-x col-x":""}">${r===2&&c===2?"Б":(r===2||c===2)?"✕":""}</i>`}).join("");const m=document.createElement("div");m.className="modal-bg";m.innerHTML=`<div class="modal"><button class="close">×</button><div class="eyebrow">${esc(t("how"))}</div><h2>${esc(t("how"))}</h2><div class="how-grid">
<div class="how-card"><h3>1. ${esc(t("learn1"))}</h3><div class="how-visual"><div class="how-board">${Array.from({length:16},(_,i)=>`<i>${i===5?"Б":""}</i>`).join("")}</div><div class="learn-piece" style="background:var(--cyan);left:86px;top:72px">Б</div></div><p>${esc(t("learn1"))}: выбери фишку персонажа.</p></div>
<div class="how-card"><h3>2. ${esc(t("learn2"))}</h3><div class="how-visual"><div class="cross-board">${crossCells}</div></div><p>${esc(t("lineText"))}</p></div>
<div class="how-card"><h3>3. ${esc(t("learn3"))}</h3><div class="how-visual"><div class="proximity"><span class="no">✕</span><span class="yes">✓</span><span class="no">✕</span><span class="yes">✓</span><span class="center">○</span><span class="yes">✓</span><span class="no">✕</span><span class="yes">✓</span><span class="no">✕</span></div></div><p>${esc(t("nearText"))}</p></div>
<div class="how-card"><h3>4. ${esc(t("learn4"))}</h3><div class="how-visual"><div style="display:flex;gap:10px;font-size:31px">🪟🔥🕯️🪴📚🗄️</div></div><p>Запрещённые предметы нельзя занимать.</p></div>
<div class="how-card"><h3>5. ${esc(t("learn5"))}</h3><div class="how-visual"><div style="display:flex;gap:14px;font-size:34px">🧶🛏️🪑</div></div><p>Ковёр, кровать, стул/кресло и свободные клетки.</p></div>
<div class="how-card"><h3>6. ${esc(t("learn6"))}</h3><div class="how-visual"><div class="how-board">${Array.from({length:16},(_,i)=>`<i>${i===4?"А":i===9?"Б":i%3===0?"✕":""}</i>`).join("")}</div></div><p>Карта показывает инициалы, крестики и последнюю клетку.</p></div>
<div class="how-card"><h3>7. ${esc(t("learn7"))}</h3><div class="how-visual"><div class="how-board">${Array.from({length:16},(_,i)=>`<i>${i===15?"☠":""}</i>`).join("")}</div></div><p>${esc(t("lastText"))}</p></div>
<div class="how-card"><h3>8. ${esc(t("learn8"))}</h3><div class="how-visual"><div class="final-example"><div class="example-board">${finalBoard}</div><div class="example-explain"><b style="color:var(--yellow)">☠</b> — жертва<br><span class="example-arrow">→</span><b style="color:var(--pink)">А</b> — единственный подозреваемый в комнате жертвы<br><br><b style="color:var(--pink)">А = убийца</b></div></div></div><p>Заполни поле → найди последнюю клетку → определи комнату жертвы → найди единственного подозреваемого в этой комнате.</p></div>
</div></div>`;document.body.appendChild(m);m.querySelector(".close").onclick=()=>m.remove()}
window.addEventListener("beforeunload",saveState);
renderHome();
})();